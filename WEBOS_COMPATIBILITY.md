# 🖥️ LG 스탠바이미 TC 호환성 분석

## 결론: **조건부 작동 가능** ⚠️

프로젝트가 LG 스탠바이미 TC에서 작동할 수 있지만, 몇 가지 문제가 있을 수 있습니다.

## LG 스탠바이미 TC 사양

### 운영체제 & 브라우저
- **OS**: webOS 6.0 기반
- **브라우저**: 독립적인 webOS TV 브라우저 (Chromium 기반)
- **앱 설치**: LG Content Store를 통해서만 가능
- **일반 웹 브라우저**: 제공되지 않음

### 주요 기능
- ✅ 터치스크린 지원
- ✅ 세로 모드 회전 가능
- ✅ 터치 리모컨 인터페이스

## 우리 프로젝트 호환성 분석

### ✅ 작동할 것으로 예상되는 기능

1. **기본 웹 렌더링**
   - Next.js 16 (React 19) - webOS Chromium에서 지원
   - Tailwind CSS - 표준 CSS이므로 문제없음
   - Framer Motion - 대부분의 모던 브라우저에서 작동

2. **Firebase 연동**
   - Firebase SDK는 webOS 브라우저에서 작동
   - Firestore, Storage, Auth 모두 지원

3. **반응형 디자인**
   - 현재 프로젝트는 반응형으로 설계됨
   - 스탠바이미 해상도에 맞춰 조정 가능

### ⚠️ 문제가 될 수 있는 기능

1. **룰렛 애니메이션** (`react-custom-roulette`)
   ```typescript
   // 문제: 복잡한 Canvas/SVG 애니메이션
   import { Wheel } from 'react-custom-roulette';
   ```
   - **이슈**: webOS 브라우저의 성능 제한으로 느릴 수 있음
   - **해결**: 애니메이션 간소화 또는 대체

2. **Web Audio API**
   ```typescript
   const AudioCtx = window.AudioContext || (window as any).webkitAudioContext;
   ```
   - **이슈**: webOS에서 Web Audio API 지원 불확실
   - **해결**: 기능 감지 후 폴백 (현재 코드에 이미 구현됨 ✅)

3. **동적 폰트 로딩** (Google Fonts)
   ```typescript
   import { Outfit, Playfair_Display, Noto_Sans_KR } from 'next/font/google';
   ```
   - **이슈**: 느린 네트워크에서 로딩 지연 가능
   - **해결**: 폰트 최적화 또는 시스템 폰트 폴백

4. **PDF 생성/뷰어**
   ```json
   "html2canvas": "^1.4.1",
   "jspdf": "^4.0.0",
   "pdfjs-dist": "^5.4.530"
   ```
   - **이슈**: 메모리 집약적 작업이 webOS에서 느릴 수 있음
   - **영향**: 티켓 생성 기능이 느리거나 실패할 수 있음

5. **터치 이벤트**
   ```typescript
   import type { PointerEvent as ReactPointerEvent } from 'react';
   ```
   - **현재 구현**: Pointer Events 사용 중 ✅
   - **스탠바이미 호환**: 터치 스크린 지원됨 ✅

### ❌ 직접 접근 불가능

1. **일반 웹 브라우저 없음**
   - 사용자가 `www.unik.ph`를 직접 입력하여 접근할 수 없음
   - **해결**: LG Content Store에 앱으로 등록 필요

2. **앱 설치 필요**
   - webOS 앱으로 패키징 필요
   - Enact Framework 또는 webOS Studio 사용

## 권장 사항

### 1. 즉시 테스트 가능한 방법

**옵션 A: webOS TV 시뮬레이터**
```bash
# webOS Studio 설치 후
npm install -g @webosose/ares-cli

# 시뮬레이터에서 URL 열기
# 또는 실제 디바이스에서 개발자 모드로 테스트
```

**옵션 B: 실제 디바이스 테스트**
1. 스탠바이미에서 개발자 모드 활성화
2. IP 주소로 직접 접근 시도
3. 브라우저가 없으므로 앱으로 패키징 필요

### 2. 최적화 권장사항

```typescript
// 1. webOS 감지 추가
const isWebOS = () => {
  if (typeof window === 'undefined') return false;
  return /webOS|Web0S/i.test(navigator.userAgent);
};

// 2. 룰렛 애니메이션 간소화
const wheelStyle = {
  pointerEvents: 'none',
  // webOS에서 성능 최적화
  ...(isWebOS() && { 
    transition: 'transform 3s cubic-bezier(0.33, 1, 0.68, 1)',
    willChange: 'transform'
  })
};

// 3. 폰트 로딩 최적화
export const metadata: Metadata = {
  // ...
  other: {
    'font-display': 'swap', // 이미 구현됨 ✅
  }
};
```

### 3. webOS 앱 변환 (필요시)

```json
// webOS 앱으로 패키징
{
  "id": "com.unik.webapp",
  "version": "1.0.0",
  "vendor": "UNIK",
  "type": "web",
  "main": "index.html",
  "title": "UNIK Tour",
  "icon": "icon.png"
}
```

## 테스트 체크리스트

### 기본 기능
- [ ] 홈페이지 로딩
- [ ] 네비게이션 동작
- [ ] 반응형 레이아웃
- [ ] 이미지 로딩

### 룰렛 페이지
- [ ] 페이지 로딩
- [ ] 룰렛 휠 렌더링
- [ ] 스핀 애니메이션
- [ ] 결과 표시
- [ ] 사운드 재생 (선택적)

### 관리자 기능
- [ ] 로그인
- [ ] Firestore 연동
- [ ] 데이터 CRUD

### 성능
- [ ] 초기 로딩 시간 < 3초
- [ ] 애니메이션 60fps
- [ ] 메모리 사용량 안정적

## 참고 자료

- [webOS TV 개발 가이드](https://webostv.developer.lge.com/)
- [webOS Studio](https://webostv.developer.lge.com/develop/tools/webos-studio)
- [Next.js webOS 배포](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)

## 최종 답변

**현재 상태로는 스탠바이미 브라우저로 직접 접근이 불가능합니다.**

하지만:
1. ✅ **기술적으로 호환 가능**: React, Firebase 등 모든 기술이 webOS에서 작동
2. ⚠️ **앱 패키징 필요**: LG Content Store에 앱으로 등록해야 사용자가 설치 가능
3. 🔧 **최적화 권장**: 성능을 위해 일부 기능 간소화 필요

실제 사용을 위해서는 **webOS 앱으로 변환하여 LG Content Store에 등록**하는 것이 가장 좋은 방법입니다.
